{
  "name": "sistema-ponto-completo",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev",
    "install-server": "cd server && npm install",
    "install-all": "npm install && npm run install-server"
  },
  "dependencies": {
    "serve": "^14.0.0"
  }
}